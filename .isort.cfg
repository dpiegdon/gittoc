[settings]
# Keep isort's output compatible with black so the two formatters don't fight
# (AGENTS.md: "isort first, then black"). Applies to every isort invocation —
# the dev pipeline, the release checklist, and ad-hoc runs.
profile = black
